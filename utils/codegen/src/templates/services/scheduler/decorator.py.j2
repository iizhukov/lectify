# ---------------------------------------------------------------------------
# Auto-generated by codegen — do not edit manually.
# To regenerate: codegen generate
# ---------------------------------------------------------------------------

from generated.scheduler.registry import register, _ScheduledTask  # type: ignore


def scheduled(
    name: str,
    cron: str | None = None,
    seconds: int | None = None,
    minutes: int | None = None,
    hours: int | None = None,
):
    """Decorator: register a function as a scheduled task.

    Args:
        name: unique task name
        cron: cron expression (e.g. "0 8 * * *")
        seconds: run every N seconds (alternative to cron)
        minutes: run every N minutes
        hours: run every N hours
    """
    if sum(x is not None for x in (cron, seconds, minutes, hours)) != 1:
        raise ValueError("Exactly one of cron/seconds/minutes/hours must be provided")

    def wrapper(func):
        task = _ScheduledTask(
            name=name,
            func=func,
            cron=cron,
            seconds=seconds,
            minutes=minutes,
            hours=hours,
        )
        register(task)
        return func

    return wrapper
