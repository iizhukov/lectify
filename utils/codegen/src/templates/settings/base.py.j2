# ---------------------------------------------------------------------------
# Auto-generated by codegen — do not edit manually.
# To regenerate: codegen generate
# ---------------------------------------------------------------------------

from pydantic_settings import BaseSettings, SettingsConfigDict
from pydantic import Field


class DatabaseSettings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="DB_", extra="ignore")

    url: str = Field(
        default="postgresql+asyncpg://lectify:lectify_password@localhost:5432/{{ service.name }}",
        description="Async database DSN",
    )
    pool_size: int = Field(default={{service.postgres.pool_size}}, ge=1)
    pool_max_overflow: int = Field(default={{service.postgres.pool_max_overflow}}, ge=0)
    pool_mode: str = Field(default="{{service.postgres.pool_mode}}")


# class KafkaSettings(BaseSettings):
#     model_config = SettingsConfigDict(env_prefix="KAFKA_", extra="ignore")

#     bootstrap_servers: str = Field(default="localhost:9092")
#     sasl_mechanism: str = Field(default="PLAIN")
#     sasl_username: str | None = Field(default=None)
#     sasl_password: str | None = Field(default=None)
#     {% if topics_consumer %}
#     consumer_group_id: str = Field(default="{# {{service.name}} #}-consumer")
#     {% endif %}


# class VaultSettings(BaseSettings):
#     model_config = SettingsConfigDict(env_prefix="VAULT_", extra="ignore")

#     addr: str = Field(default="http://localhost:8200")
#     token: str | None = Field(default=None)
#     mount_point: str = Field(default="{# {{service.vault.mount_point}} #}")


class MinioSettings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="MINIO_", extra="ignore")

    endpoint: str = Field(default="localhost:9000")
    access_key: str = Field(default="minioadmin")
    secret_key: str = Field(default="minioadmin")
    region: str = Field(default="{# {{service.minio.region}} #}")


# class AuthSettings(BaseSettings):
#     model_config = SettingsConfigDict(env_prefix="AUTH_", extra="ignore")

#     addr: str = Field(default="http://localhost:9001")
#     ticket_ttl_seconds: int = Field(default=60)


# class ConfigServiceSettings(BaseSettings):
#     model_config = SettingsConfigDict(env_prefix="CONFIG_", extra="ignore")

#     addr: str = Field(default="http://localhost:9002")


# class ObservabilitySettings(BaseSettings):
#     model_config = SettingsConfigDict(env_prefix="OBS_", extra="ignore")

#     otlp_endpoint: str = Field(default="http://localhost:4317")
#     log_level: str = Field(default="{# {{service.observability.log_level}} #}")
#     trace_sampling_rate: float = Field(default={# {{service.observability.trace_sampling_rate}} #})
#     metrics_enabled: bool = Field(default=True)


class GrpcServerSettings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="GRPC_", extra="ignore")

    port: int = Field(default={{ service.grpc.port }})
