resolver 127.0.0.11 valid=10s ipv6=off;

# Grafana
server {
    listen 80;
    server_name grafana.localhost;

    location / {
        proxy_pass http://lectify-grafana:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
}

# Vault
server {
    listen 80;
    server_name vault.localhost;

    location / {
        proxy_pass http://lectify-vault:8200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# MinIO Console (с поддержкой WebSocket)
server {
    listen 80;
    server_name minio.localhost;

    # Основной прокси с поддержкой WebSocket
    location / {
        proxy_pass http://lectify-minio:9001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
    }
}

# MkDocs docs
server {
    listen 80;
    server_name docs.localhost;

    location / {
        proxy_pass http://lectify-mkdocs:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Kafka UI
server {
    listen 80;
    server_name kafka.localhost;

    location / {
        proxy_pass http://lectify-kafka-ui:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Prometheus
server {
    listen 80;
    server_name prometheus.localhost;

    location / {
        proxy_pass http://lectify-prometheus:9090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Alertmanager
server {
    listen 80;
    server_name alertmanager.localhost;

    location / {
        proxy_pass http://lectify-alertmanager:9093;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Tempo
server {
    listen 80;
    server_name tempo.localhost;

    location / {
        proxy_pass http://lectify-tempo:3200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Catch-all: root → docs
server {
    listen 80 default_server;
    server_name _;

    location = / {
        return 302 http://docs.localhost;
    }

    location / {
        return 404;
    }
}